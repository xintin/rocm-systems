@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(NOT @DISABLE_COMGR@)
    find_dependency(amd_comgr CONFIG)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/rocprof-trace-decoder-targets.cmake")

check_required_components(rocprof-trace-decoder)
